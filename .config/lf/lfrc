cmd open-within %{{
    case $(file --brief --mime-type "$f") in
        text/*) lf -remote "send $id push e" ;;
        inode/*) lf -remote "send $id push l" ;;
        *) devour xdg-open "$f" ;;
    esac
}}

cmd say-no ${{
    echo no >&5
}}

map <enter> open-within

map Q quit
map q :say-no; quit
